name: flutter_games_library
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.2.0
  provider: ^6.0.5
  lottie: ^3.3.3
  flutter_svg: ^2.3.0
  envied: ^1.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  envied_generator: ^1.3.5
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/icons/